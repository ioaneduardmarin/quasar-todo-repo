import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eduardmarin.todo',
  appName: 'quasar-todo',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
